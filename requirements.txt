# Core ML
torch>=2.0.0
numpy>=1.24.0

# Web Server
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=11.0
pydantic>=2.0.0

# Utilities
requests>=2.28.0

# Comparison Mode (optional — GPT-2 via TransformerLens)
# Install only if using the ⚡ Compare feature:
# pip install transformer-lens
transformer-lens>=1.0.0
